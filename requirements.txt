# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
pyarrow>=12.0  # parquet caching

# Data sources (free, no credentials)
yfinance>=0.2.40
# pandas-datareader is optional; Fama-French factors are loaded via direct CSV
# download from the Ken French Data Library (see src/data.py) to avoid the
# recurring pandas/pandas-datareader version-compatibility breakage.
pandas-datareader>=0.10

# Testing
pytest>=7.0

# Optional: best-effort Markdown->PDF for report/build_pdf.py (use pandoc if available)
fpdf2>=2.7
